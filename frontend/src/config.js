// 功能配置（前端可用来隐藏不支持的功能）
// 实际可从后端/本地存储拉取，这里提供默认
export const featureConfig = {
  showDashboard: true,
  showControl: true,
  showTTS: true,
  showASR: true,
  showAI: true,
  showAudio: true,
  showLogs: true,
  showSettings: true,
}
